# Reactive System Environment Variables

# ===========================================
# System-Wide Latency Bounds (CQRS)
# ===========================================
# These control the latency SLA for the entire system
# MIN: Don't process snapshots more frequently than this (aggregate events)
# MAX: Always process within this bound (never delay more than this)
LATENCY_MIN_MS=1
LATENCY_MAX_MS=100

# ===========================================
# Kafka Configuration
# ===========================================
KAFKA_BROKERS=kafka:29092
KAFKA_EXTERNAL_BROKERS=localhost:9092

# Service Ports
UI_PORT=3000
GATEWAY_PORT=8080
FLINK_PORT=8081
DROOLS_PORT=8180
KAFKA_PORT=9092
ZOOKEEPER_PORT=2181

# Kafka Topics
COUNTER_EVENTS_TOPIC=counter-events
COUNTER_RESULTS_TOPIC=counter-results
COUNTER_ALERTS_TOPIC=counter-alerts

# Java Settings
JAVA_OPTS=-Xmx512m

# Node Settings
NODE_ENV=development
