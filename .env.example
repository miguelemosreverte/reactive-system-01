# Reactive System Environment Configuration
# Copy this file to .env and modify as needed
# All values shown are defaults - only override what you need

# ===========================================
# System-Wide Latency Bounds (CQRS)
# ===========================================
# These control the latency SLA for the entire system
# MIN: Don't evaluate snapshots more frequently than this (aggregate events)
# MAX: Always evaluate within this bound (guaranteed SLA)
# LATENCY_MIN_MS=1
# LATENCY_MAX_MS=100

# ===========================================
# Environment Mode
# ===========================================
# Set to 'production' to disable auto-benchmark and development features
NODE_ENV=development

# ===========================================
# Security
# ===========================================
# API key for admin endpoints (trace queries, benchmark control)
ADMIN_API_KEY=reactive-admin-key

# Grafana admin credentials
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin

# ===========================================
# Observability (optional)
# ===========================================
# Set to 'false' to disable OpenTelemetry tracing
# Default sampling is 0.1% - use X-Debug:true header for full tracing per-request
OTEL_FEATURE_ENABLED=true
