name: "HTTP - Spring WebFlux"
description: "Enterprise framework, Netty under the hood - BASELINE"
component: http
duration: 60000  # 1 minute
concurrency: 500

# Expected throughput from documented benchmarks (HTTP_SERVER_BENCHMARKS.md)
# Spring: 10,623 req/s - enterprise framework overhead
# This is the BASELINE - all other implementations should beat this
expectedThroughput: 10623
expectedThroughputNote: "Documented baseline - enterprise framework overhead"

config:
  httpServer: SPRING_BOOT
  microbatching: false
  microbatchSize: 0
  microbatchTimeoutMs: 0
  kafkaAcks: "1"
  kafkaBatchSize: 16384
  kafkaLingerMs: 0
