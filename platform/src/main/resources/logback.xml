<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Suppress verbose Kafka client logging -->
    <logger name="org.apache.kafka" level="WARN"/>
    <logger name="org.apache.kafka.clients.NetworkClient" level="ERROR"/>
    <logger name="org.apache.kafka.clients.producer.internals.Sender" level="ERROR"/>
    <logger name="org.apache.kafka.clients.Metadata" level="ERROR"/>
    <logger name="io.opentelemetry" level="WARN"/>

    <!-- Platform benchmark logging -->
    <logger name="com.reactive.platform.benchmark" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
